import { media, menor, maior } from './moduloNotas.mjs';

const notas = [8, 6, 9, 7, 10, 5];
console.log(`Média: ${media(notas)}`);
console.log(`Menor Nota: ${menor(notas)}`);
console.log(`Maior Nota: ${maior(notas)}`);
